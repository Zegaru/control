# Implementation Plans

Originally generated by the improve skill on 2026-07-19 against commit `f1729ab`. OSS release pack (013–016) added 2026-07-22 against commit `5cb94aa`. Perf pack (017–021) and perf pack 2 (022–025) landed 2026-07-22 against commit `0724957`. Perf pack 3 (026–031) landed 2026-07-22. UI loading-states pack (**033–035**) added 2026-07-22 against commit `81b2809`. Direction pack (**032**, **036–038**) added 2026-07-22 against commit `81b2809` (env files, `.claude/launch.json`, hide UI, past-run logs). DX clone-to-dev pack (**039–041**) added 2026-07-22 against commit `f47f402` (daemon watch, split-terminal port sync, test:watch + CONTRIBUTING). Execute open plans in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Plans **001–012** are historical (DONE). **013–016** are the open-source launch pack (DONE). **017–025** are perf packs 1–2 (DONE). **026–031** are perf pack 3 (DONE). **033–035** are UI query loading (DONE). **032** + **036–038** are the open direction pack (DONE). **039–041** are the DX clone-to-dev pack (DONE).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add Vitest verification baseline + characterization tests | P1 | M | — | DONE |
| 002 | Make adopted runs stoppable; tighten reconcile (PID required) | P1 | M | 001 | DONE |
| 003 | Duplicate-start guard, `unhealthy` demotion, fail group waits | P1 | M | 001 | DONE |
| 004 | Loopback bind, healthUrl allowlist, scoped container log WS | P1 | M | 001 | DONE |
| 005 | Cached metrics HTTP + AgentStatus without 55ms React churn | P2 | S | — | DONE |
| 006 | Coalesce Overview host/Docker sampling; narrow WS/SQL storms | P2 | L | 005, 001 | DONE |
| 007 | Tauri shell: adopt existing daemon; sane Quit/Restart | P2 | M | — | DONE |
| 008 | Refresh DESIGN/README/UI design docs to match M0–M6 | P2 | S | — | DONE |
| 009 | AGENTS.md + lint/format + GitHub Actions CI | P2 | M | 001 | DONE |
| 010 | Direction spikes: env files, PTY attach, detection, ports | P3 | L | 008, 001 | DONE |
| 011 | Optional PTY attach + fix xterm copy | P2 | M | — | DONE |
| 012 | Per-project port labels (Dashboard + Port Map) | P2 | M | — | DONE |
| 013 | MIT LICENSE + package license fields + SECURITY.md | P1 | S | — | DONE |
| 014 | README platforms, prerequisites, install paths, screenshots scaffold | P1 | M | 013 | DONE |
| 015 | CONTRIBUTING, Code of Conduct, GitHub issue/PR templates | P1 | S | 013, 014 (soft) | DONE |
| 016 | CI on windows-latest as well as ubuntu-latest | P1 | M | — | DONE |
| 017 | Stabilize LogPanel subscribe callbacks (no xterm remount) | P2 | S | — | DONE |
| 018 | Gate CommandPalette `useAllActions` when palette closed | P2 | S | — | DONE |
| 019 | Coalesce hostPorts with shared process snapshot | P2 | M | — | DONE |
| 020 | Batch SQLite hot paths + compose matcher TTL cache | P2 | M | — | DONE |
| 021 | Cap live log snapshots + ring buffer oversized chunk | P2 | S | — | DONE |
| 022 | Docker stats TTL + project-metrics sample in-flight guard | P2 | M | — | DONE |
| 023 | Cheap claimedPorts without full port map | P2 | S | — | DONE |
| 024 | Remove Tauri beforeDevCommand UI production build | P2 | S | — | DONE |
| 025 | Tray aggregate GET /api/tray + cheap project counts | P2 | S | — | DONE |
| 026 | Bulk GET /projects/trees + projectId on run.status WS | P2 | M | — | DONE |
| 027 | Split event log from socket context (no app re-render) | P2 | S | — | DONE |
| 028 | Lazy-load views; remove motion; defer xterm CSS | P2 | S | — | DONE |
| 029 | Calm Overview polling + skip unchanged metric history | P2 | S | — | DONE |
| 030 | Cap dead-run log tail read (256 KiB from EOF) | P2 | S | — | DONE |
| 031 | CI: typecheck/lint once; test on OS matrix | P2 | S | — | DONE |
| 032 | Per-action `.env` / `.env.local` selection at spawn | P2 | M | — | DONE |
| 033 | Kit Skeleton / ViewLoading / PanelLoading primitives | P2 | S | — | DONE |
| 034 | Query loading vs empty vs error (Ports, Groups, Docker, Settings, App, ProjectDetail) | P2 | M | 033 | DONE |
| 035 | Overview cold-load project card skeletons | P2 | S | 033 | DONE |
| 036 | Import `.claude/launch.json` as detected actions | P2 | M | — | DONE |
| 037 | Hide action / module controls in the UI | P2 | S | — | DONE |
| 038 | Open past-run logs from ActionEditor history | P2 | S | — | DONE |
| 039 | Daemon `dev` uses `tsx watch` (auto-restart) | P2 | M | — | DONE |
| 040 | Split-terminal port sync + `.env.example` / env docs | P2 | M | — | DONE |
| 041 | `test:watch` + CONTRIBUTING / AGENTS Dev loop | P2 | S | 039 (soft 040) | DONE |

## Direction spikes (`plans/spikes/`)

| Spike | Recommendation |
|-------|----------------|
| [env-files](spikes/env-files.md) | Plan **032** (TODO) |
| [pty-attach](spikes/pty-attach.md) | Done — [011](011-pty-attach-and-xterm-copy.md) |
| [detection-markers](spikes/detection-markers.md) | Plan **036** = `.claude/launch.json` only; workspace/just/Expo still open |
| [cross-platform-ports](spikes/cross-platform-ports.md) | Defer; parser tests first |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (one-line reason) | `REJECTED` (one-line rationale)

## Finding → plan map

| Audit # | Finding | Plan |
|--------:|---------|------|
| 1 | Test / verification baseline | 001 |
| 2 | Adopted runs unstoppable | 002 |
| 3 | Duplicate action starts | 003 |
| 4 | `unhealthy` never set | 003 |
| 5 | Reconcile `alive \|\| portsUp` | 002 |
| 6 | Non-loopback `CONTROL_HOST` | 004 |
| 7 | Unrestricted `healthUrl` fetch | 004 |
| 8 | Group wait timeout / failed step continues | 003 |
| 9 | Metrics re-sample on HTTP | 005 |
| 10 | Shell daemon orphan / false adopt | 007 |
| 11 | WS invalidates trees+ports every status | 006 |
| 12 | Overlapping Win32 PowerShell scans | 006 |
| 13 | Container log subscribe unscope | 004 |
| 14 | Stale DESIGN / README | 008 |
| 15 | No AGENTS.md / CI (+ lint) | 009 |
| — | Direction: env files, PTY, detection, cross-platform ports | 010 |
| — | PTY attach + xterm clipboard/copy | 011 |
| — | Per-project port labels | 012 |
| — | Docker list fan-out + registry N+1 active runs | 006 |
| — | AgentStatus 55ms setState loop | 005 |
| — | No LICENSE / SPDX / package license fields | 013 |
| — | No SECURITY.md / consumer threat model | 013 |
| — | README gaps for external installers | 014 |
| — | No CONTRIBUTING / CoC / issue-PR templates | 015 |
| — | Ubuntu-only CI vs Windows-first product | 016 |
| — | LogPanel xterm remount on run.status | 017 |
| — | CommandPalette always-on tree queries | 018 |
| — | Dual Win32_Process PowerShell scans | 019 |
| — | SQLite N+1 on ports/list/tree/metrics | 020 |
| — | Unbounded live log REST/WS payloads | 021 |
| — | Per-container docker.stats every metrics tick | 022 |
| — | claimedPorts rebuilds full port map on start | 023 |
| — | Tauri beforeDevCommand full UI build | 024 |
| — | Tray snapshot 4 serial HTTP + heavy /projects | 025 |
| — | N HTTP tree fetches on Overview / palette | 026 |
| — | run.status re-renders whole app via events in socket | 027 |
| — | Eager view bundle + global xterm CSS + unused motion | 028 |
| — | Aggressive Overview poll cadence | 029 |
| — | Full-file readFileSync on dead-run log tail | 030 |
| — | Redundant typecheck/lint on both CI matrix legs | 031 |
| L1 | No shared loading primitive in kit | 033 |
| L2 | False empty states on query pending (Ports/Groups/Docker/Settings) | 034 |
| L3 | Bare `Loading…` text (App Suspense, ProjectDetail) | 034 |
| L4 | ProjectDetail failed tree looks like forever-loading | 034 |
| L5 | Overview cold load shows only Add Project card | 035 |
| D1 | Per-action `.env` file loading (DESIGN §12 deferred) | 032 |
| D2 | `.claude/launch.json` detection (DESIGN §6) | 036 |
| D3 | Hide action/module UI (API ready, UI missing) | 037 |
| D4 | Past-run log open from ActionEditor history (FR-5/13) | 038 |
| DX1 | Daemon `dev` has no file watch / auto-restart | 039 |
| DX2 | Split-terminal `dev:daemon` + `dev:ui` port desync | 040 |
| DX5 | Undocumented `CONTROL_DAEMON_URL` / `CONTROL_DEV`; missing `.env.example` | 040 |
| DX4 | Hot-reload / restart behavior undocumented | 041 (docs; 039/040 supply facts) |
| DX3 | No `test:watch`; CONTRIBUTING claims Biome formats | 041 |

## Dependency notes

- **001 before 002/003/004/006/009/010**: later plans expect `pnpm test` and extend characterization tests.
- **002 pairs reconcile + stop**: do not ship stop-by-PID without fixing port-only false adopts (same plan).
- **005 before 006**: remove double HTTP sampling before investing in deeper coalescing.
- **004 does not add auth**: localhost trust remains; it only prevents escaping that trust model.
- **007 is independent** of JS daemon plans but complements 002 (process adoption vs run adoption).
- **008 before 010**: spikes should cite honest backlog labels.
- **009 needs 001**: CI runs `pnpm test`.
- **003 and 002** can proceed in parallel after 001; if `force` restart must stop adopted runs, prefer 002 first or keep force limited to live handles (003 STOP text).
- **011** does not depend on env-files (operator pulled attach + xterm copy ahead of the spike deferral). Env-files is plan **032**.
- **033 before 034/035**: kit primitives must exist before views import them.
- **034 and 035** can run in parallel after 033; prefer 034 first if stacking one branch.
- **033–035** are UI-only; do not start `pnpm dev` unless the operator asks.
- **011** must not grow into a free-standing shell or PTY resize — see plan STOP / out of scope.
- **012** is independent: project JSON `portLabels` + port-map overlay; does not touch host port scanning.
- **013 before 014/015**: LICENSE + SECURITY.md should exist before README/Contributing link to them.
- **014 before 015** is soft: CONTRIBUTING can land first, but README should end linking a real `CONTRIBUTING.md`.
- **016 is independent** of 013–015 and can run in parallel with docs work.
- **OSS deferred (not in 013–016)**: CHANGELOG + GitHub Releases + Authenticode signing; third-party NOTICE for NSIS bundle; `repository` fields when a remote exists (013 skips inventing URLs).
- **032 and 038** both edit `ActionEditor.tsx` — serialize those two (prefer **032 → 038**).
- **036** is scanner-only; safe in parallel with 032/037/038.
- **037** is UI-only (`ActionRow` / `ProjectDetail` / `api.patchModule`); soft-conflict with 038 on `ActionRow` (pass-through prop) — prefer **037 before 038** if stacking.
- **032 / 036 / 037 / 038** do not start `pnpm dev` unless the operator asks.
- **039 and 040** both edit `scripts/run-daemon-dev.mjs` — serialize or rebase (prefer **039 → 040**).
- **041** depends on **039** for accurate daemon-watch Dev loop text; soft-depends on **040** for env / `.control-dev-port` wording.
- **039 / 040 / 041** do not start `pnpm dev` unless the operator asks; do not enable Biome formatter.

Suggested parallel tracks for the DX pack:

```
Track A: 039 → 040 → 041
# or 039 ∥ 040 (rebase run-daemon-dev), then 041
```

Suggested parallel tracks for the direction pack:

```
Track A: 032 → 038
Track B: 036
Track C: 037 → (rebase 038 if needed)
```

## Findings considered and rejected / deferred

| Item | Disposition |
|------|-------------|
| Missing auth on loopback | **Rejected** — DESIGN NFR-2 / localhost trust |
| Shelling out user action commands | **Rejected** — core product |
| Non-Windows ports as a *bug* | **Deferred to 010** as direction (NFR-1) |
| Zod v3 → v4 migration | **Deferred** — not worth blocking correctness; revisit when shared schemas churn heavily |
| Split `registry.ts` / `kit.tsx` / Dashboard god-modules | **Deferred** — high merge risk; do **after** 001 characterization + 006 batching; open a new plan if still painful |
| Compose project name collision (MED confidence) | **Deferred** — re-audit after 002/power-stop usage; not planned until reproduced |
| Full interactive terminal product | **Rejected** as scope — PTY spike in 010 is attach-only; 011 implements attach + copy, not a free shell |
| Signed NSIS + release automation | **Deferred** — ship source-first; add when ready for tagged Windows installers |
| NOTICE / third-party attribution for installer | **Deferred** — needed before widely distributing signed/unsigned installers |
| Removing or hiding `plans/` from the public tree | **Deferred** — label in README (014); not worth relocating yet |
| Mutation busy labels (`Saving…`, `Scanning…`, rocker amber) | **Rejected** — already appropriate pending feedback; loading audit scoped to query/empty conflation |
| Skeleton entire Overview (metrics, event log) while any query pending | **Deferred** — 035 covers project cards only |
| RunDrawer / ContainerDrawer idle LED until row resolves | **Deferred** — low impact; revisit if drawers feel broken; **038** loads `GET /runs/:id` for dead-run header metadata |
| Desktop / ControlStrip notifications (`_notifications`) | **Deferred** — direction follow-up; not in 032/036–038 |
| Workspace glob expansion / justfile targets / richer Expo | **Deferred** — after **036**; see detection-markers spike |
| macOS/Linux host ports | **Deferred** — cross-platform-ports spike; NFR-1 |
| Enable Biome formatter repo-wide | **Deferred** — large churn; 041 only fixes CONTRIBUTING wording + `lint:fix` |
| Pre-commit / husky hooks | **Deferred** — already deferred in 009; CI remains the gate |
| `.editorconfig` / `.nvmrc` / VS Code extensions.json | **Deferred** — low leverage vs watch/port/docs |
| Tauri shell hardcoded `:4400` ignoring port bumps | **Deferred** — shell-contributor only; not in 039–041 |
| Native-module install troubleshooting section | **Deferred** — document when Windows first-install pain recurs |
| Daemon graceful stop of all supervised runs on SIGTERM | **Deferred** — watch restart tradeoff documented in 039 only |

## How to execute

```bash
# Example
git checkout -b advisor/001-verification-baseline
# follow plans/001-verification-baseline.md
# update this README status cell → DONE
```

For advisor-dispatched executors: use isolated worktrees / `execute <plan>` flow if available; do not push or open PRs unless the operator asks.
