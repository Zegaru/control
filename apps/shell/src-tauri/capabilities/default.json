{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "CONTROL shell capabilities. The main window navigates to the daemon-served UI at 127.0.0.1:4400. Tray + Settings can toggle autostart; dialog:allow-open lets the UI open a native folder picker when adding a project.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:4400/*"]
  },
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
